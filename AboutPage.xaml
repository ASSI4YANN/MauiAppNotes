<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MauiAppNotesFormation.AboutPage"
             Title="A propros">

    <ContentPage.ToolbarItems>
        <ToolbarItem IconImageSource="share_icon.png" Text="Partager" Clicked="ToolbarItem_Clicked" />
    </ContentPage.ToolbarItems>

    <VerticalStackLayout Spacing="10" Margin="10">
        <HorizontalStackLayout Spacing="10">
            <Image Source="icon_notes.png"
                   SemanticProperties.Description="the dot net bot waving hello!"
                   HeightRequest="64"/>
            <Label  FontSize="22"
                    FontAttributes="Bold"
                    Text=" APP Notes"
                    VerticalOptions="End"/>

            <Label  FontSize="22"
                    Text="v1.0"
                    VerticalOptions="End"/>
        </HorizontalStackLayout>

        <Label FontAttributes="Bold" FontSize="15" Text="•Application de prise de notes | Développeur en c# avec MAUI : N'GUESSAN ASSI YANN JUNIOR "/>

      
    </VerticalStackLayout>
</ContentPage>